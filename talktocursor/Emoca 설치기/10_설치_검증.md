# 10단계: 설치 검증

## 작업 완료 시간
2025년 2월 26일

## 검증 항목

### 1. Python 버전
```bash
conda activate work38_cu11
python --version
```
**예상 결과:** Python 3.8.20

### 2. PyTorch
```bash
export LD_LIBRARY_PATH=$CONDA_PREFIX/lib:$LD_LIBRARY_PATH
python -c "import torch; print(f'PyTorch: {torch.__version__}'); print(f'CUDA available: {torch.cuda.is_available()}'); print(f'CUDA version: {torch.version.cuda}')"
```
**예상 결과:**
- PyTorch: 1.12.1+cu113
- CUDA available: True
- CUDA version: 11.3

### 3. PyTorch3D
```bash
python -c "import pytorch3d; print(f'PyTorch3D: {pytorch3d.__version__}')"
```
**예상 결과:** PyTorch3D: 0.7.2

### 4. mediapipe
```bash
python -c "import mediapipe; print(f'mediapipe: {mediapipe.__version__}')"
```
**예상 결과:** mediapipe: 0.10.5

### 5. GDL
```bash
python -c "import gdl; import gdl_apps; print(f'GDL 경로: {gdl.__file__}'); print('gdl_apps import 성공')"
```
**예상 결과:**
- GDL 경로: /media/cine/First/HWPJ2/NewProject/External Projects/emoca/gdl/__init__.py
- gdl_apps import 성공

### 6. 주요 패키지 버전 확인
```bash
pip list | grep -E "(torch|pytorch3d|mediapipe|numpy|protobuf|omegaconf|face-alignment)"
```

**예상 결과:**
- torch: 1.12.1+cu113
- pytorch3d: 0.7.2
- mediapipe: 0.10.5
- numpy: 1.23.5
- protobuf: 3.20.3
- omegaconf: 2.0.6
- face-alignment: 1.3.6

## 통합 검증 스크립트

```bash
export CONDA_PKGS_DIRS=/media/cine/First/HWPJ2/Temp/conda_pkgs
export TMPDIR=/media/cine/First/HWPJ2/Temp
export TEMP=/media/cine/First/HWPJ2/Temp
export PIP_CACHE_DIR=/media/cine/First/HWPJ2/Temp/pip_cache
export LD_LIBRARY_PATH=/home/cine/anaconda3/envs/work38_cu11/lib:$LD_LIBRARY_PATH

source /home/cine/anaconda3/etc/profile.d/conda.sh
conda activate work38_cu11

echo "=== 설치 검증 ==="
echo "1. Python: $(python --version)"
echo "2. PyTorch: $(python -c 'import torch; print(torch.__version__)')"
echo "3. PyTorch3D: $(python -c 'import pytorch3d; print(pytorch3d.__version__)')"
echo "4. mediapipe: $(python -c 'import mediapipe; print(mediapipe.__version__)')"
echo "5. GDL: $(python -c 'import gdl; print(\"OK\")')"
echo "✅ 모든 검증 완료!"
```

## 설치 완료 확인

모든 단계가 완료되면 다음 명령어로 EMOCA 실행 준비가 되었는지 확인할 수 있습니다:

```bash
cd "/media/cine/First/HWPJ2/NewProject/External Projects/emoca"
conda activate work38_cu11
export LD_LIBRARY_PATH=$CONDA_PREFIX/lib:$LD_LIBRARY_PATH

python ./gdl_apps/EMOCA/demos/test_emoca_on_images.py --help
```

## 다음 단계
모든 설치가 완료되었습니다! 이제 EMOCA를 사용할 수 있습니다.

