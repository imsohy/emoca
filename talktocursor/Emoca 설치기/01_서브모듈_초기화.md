# 1단계: 서브모듈 초기화

## 작업 완료 시간
2025년 2월 26일

## 작업 내용

### 1. 기본 서브모듈 초기화
```bash
cd "/media/cine/First/HWPJ2/NewProject/External Projects/emoca"
bash pull_submodules.sh
```

**초기화된 서브모듈:**
- `external/emonet`
- `external/SwinTransformer`
- `external/face-parsing.PyTorch`
- `external/spectre` (하위 서브모듈 포함)

### 2. 추가 서브모듈 초기화
```bash
git submodule update --init external/Deep3DFaceRecon_pytorch
git submodule update --init external/TDDFA_V2
```

**문제 발생:**
- `Deep3DFaceRecon_pytorch`의 요구 커밋이 원격 저장소에 없음
- **해결**: master 브랜치로 체크아웃
  ```bash
  cd external/Deep3DFaceRecon_pytorch
  git fetch origin master
  git checkout master
  ```

### 3. 서브모듈 상태 확인
```bash
git submodule status
```

**최종 상태:**
```
+da7efde87b4cd5f4346394d5e7f0ac217906a0ab external/Deep3DFaceRecon_pytorch (heads/master)
 a510b0c2200af663cee56d6df7257efacd1c5981 external/SwinTransformer (a510b0c)
 9fdbea1eb97f762221f71f5c76f08f52296c6704 external/TDDFA_V2 (v0.12-9-g9fdbea1)
 57ac8e9fc659994b9f737351723abcf6a24153a4 external/emonet (remotes/origin/feature/exposeEemotionFeature)
 d2e684cf1588b46145635e8fe7bcc29544e5537e external/face-parsing.PyTorch (heads/master)
 bdc235b49b0c3aafa1c9f05379f8e415047abf0d external/spectre (heads/master)
```

## 알려진 문제

### face_detection LFS 파일
- **위치**: `external/spectre/external/face_detection/ibug/face_detection/retina_face/weights/Resnet50_Final.pth`
- **문제**: Git LFS 예산 초과로 실제 파일 다운로드 실패
- **상태**: Git LFS 포인터 파일만 존재 (134 bytes)
- **해결**: 실제로는 사용되지 않는 것으로 확인, EMOCA 실행에는 필수적이지 않을 수 있음
- **참고**: `mobilenet0.25_Final.pth`는 정상적으로 존재 (1.8MB)

## 검증

모든 주요 서브모듈이 정상적으로 초기화되었고, 디렉토리 구조가 올바르게 생성되었습니다.

```bash
ls -la external/
# Deep3DFaceRecon_pytorch, emonet, face-parsing.PyTorch, spectre, SwinTransformer, TDDFA_V2 모두 존재 확인
```

## 다음 단계
2단계: Conda 환경 생성

